# Store

Global client state should live here when QCFlow needs shared state that cannot stay local to a page.

Current project state is mostly local React state, so this directory is intentionally empty.

